language: c
os:
  - linux
script: make all check
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - xmlto
      - gcc-7

matrix:
  include:
    - os: linux
      compiler: gcc
      dist: trusty
    - os: linux
      compiler: gcc
      dist: trusty
      env: CC=gcc-7

